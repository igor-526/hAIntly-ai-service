# Deployment settings
name: haintly-ai-service
componentsLabel: haintly-ai-service
replicaCount: 1
namespace: haintly

# App settings
secretName: haintly-ai-service-secret
containerPort: 8000
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

# Image settings
image: ghcr.io/igor-526/haintly_ai-service
imagePullSecertName: ghcr-secret
imageTag: latest

migration:
  command: ["uv", "run", "alembic", "-c", "alembic.ini", "upgrade", "head"]
  workingDir: /app/src